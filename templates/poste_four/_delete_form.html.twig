<form method="post" action="{{ path('app_poste_four_delete', {'id': poste_four.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poste_four.id) }}">
    <button class="btn">Delete</button>
</form>
